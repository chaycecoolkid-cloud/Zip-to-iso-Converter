Bug Fixes

v1.0.1- Added a preflight check that prints a very clear “why this host is incompatible” 
summary before it starts anything, so the user knows immediately whether the environment is suitable.

